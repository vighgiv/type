export enum AuthCode {
  SUCCESS_REGISTRATION = 'success-register',
  SUCCESS_RESET = 'success-reset',
  INVALID_LOGIN_CREDENTIALS = 'invalid-credentials',
  INVALID_REGISTER_CREDENTIALS = 'invalid-register',
  INVALID_EMAIL = 'invalid-email'
}
